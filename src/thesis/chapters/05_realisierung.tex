\chapter{Realisierung}

\section{Website Fingerprinting Versuchsaufbau Stand der Technik}

Als Voraussetzung wird ein Webserver benötigt welche eine Menge an definierten Websiten bereitstellt.
In dem Paper von Jansen wurde aufgezeigt dass dies mit der zimply Python Library erreicht werden kann.
Diese Bibliothek stellt einen Webserver für ZIM Dateien zur Verfügung.
\cite[S. 4]{jansen2023data} 
\footnote{\url{https://github.com/kimbauters/ZIMply}}


Jansen und Wails haben gezeigt, dass \texttt{wget2} als Surrogat für den Tor Browser verwendet werden kann.
Sowohl im Live-Tor-Netzwerk als auch in einer Shadow-Simulation wiesen die erzeugten Tor-Zellsequenzen hinreichende Ähnlichkeit für Website-Fingerprinting-Analysen auf. \cite[S.~6]{jansen2023data}


\section{Website Fingerprinting Versuchsaufbau Experiment}

Die vorliegende Arbeit beschränkt sich auf ein Closed-World-Szenario mit einer einzelnen Netzwerkkonfiguration.
Diese Einschränkung ist vertretbar, da Jansen und Wails gezeigt haben, dass die Closed-World-Klassifikationsgenauigkeit bei variierender Netzbelastung nur um 1--3\,\% schwankt \cite[S. 11]{jansen2023data}.

//TODO: ich glaube wir fokussieren uns nur auf die richtung der Zellen und nicht auf das Timing.
//Diesen Umstand noch besser erklären
//Welcher Classifier wird genutzt?

\section{Versuchsaufbaue Simulation}
//TODO: Eine bewertung der anderen verfügbaren Tools vornehmen und Argumentieren weshalb der jetztige setup genommen wird. auch shadow mit einer iegenen simulation oder echten daten / echtes netzwerk. jansenshadow und neverenough und andere quellen hinzuziehen und fertig argumenetierne.
Um eine Simulation des Website Fingerprintings Angriff durchzuführen nutzen wir Shadow mit Tornettools.
Damit werden 

For your thesis, this is worth mentioning briefly in your threat model section — something like: "We assume the attacker can identify Tor traffic, which is realistic since public relay IPs are published in the Tor consensus. The attacker's goal is not to detect Tor usage but to determine which website is being accessed through Tor." This aligns with all the papers you've been reading.

\subsubsection{Simulationssetup und Entscheidungen}

Die eavesdrop attacke wird auf dem client ausgeführt.
Es werden Pcap files extrahiert und diese genutzt.

Alternativ könnte man die Tor Netzwerk binaries verwenden und direkt informationen daraus ziehen wie bei jansen beschrieben.
Dagegen entschieden da es Aufwand ist extra die Tor Binaries anzupassen.


\todo{Simulation mehrmals laufen lassen. This is important for statistical validity — the "Once is Never Enough" methodology from the Jansen et al. 2021 paper}
\todo{Genauer Aufbau mit den Clients und so bescchreiben. This is why virtually all WF research — including Jansen \& Wails in Shadow — uses a dedicated client that only does the target fetches. It's a simplifying assumption that the entire field makes. The research community knows it's optimistic for the attacker, and there are papers specifically studying what happens when you relax it (multi-tab WF, like the Deng et al. IEEE 2023 paper on "Robust Multi-tab Website Fingerprinting Attacks in the Wild"). For your thesis, I'd recommend sticking with the clean setup — dedicated WF client, one fetch at a time, circuit isolation. There are two reasons. First, it's what the literature does, so your results are directly comparable to prior work. Second, your thesis variable is circuit padding, not traffic multiplexing. You want to isolate the effect of padding on DF accuracy. If you also add client-side background traffic, you've introduced a second variable and can't tell whether accuracy dropped because of padding or because of traffic mixing.}
\todo{Wie viel Traffic und wie viele Relays werden generiert? Mit was für einem Scale arbeiten wir.}
\todo{Statistische Interpreation der Resultate mit Outliers und so ?}


\todo{Die mutierte wget2 bibliothek von dem jansen & wails paper verwenden}
\todo{Der Client selbst produziert keinen traffic}